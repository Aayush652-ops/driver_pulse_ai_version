"""
Page modules for the DriverPulse AI application.

Each Streamlit page is implemented in its own module with a `run`
function accepting the current driver's ID as a parameter. The main
application imports and calls these `run` functions based on user
navigation selection.
"""
